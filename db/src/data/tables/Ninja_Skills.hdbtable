COLUMN TABLE "NINJA_SKILLS"(
    "RESIDENT_ID" INTEGER COMMENT 'Resident ID',
    "SKILL_ID" INTEGER COMMENT 'SKILL ID'
)WITH ASSOCIATIONS( JOIN "RESIDENTS" as "RES" on "RESIDENT_ID" = "RES"."RESIDENT_ID",
                    JOIN "SKILLS" as "SKL" on "SKILL_ID" = "SKL"."SKILL_ID" )

