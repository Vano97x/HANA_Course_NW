COLUMN TABLE "VILLAGE"(
    "VILLAGE_ID" INTEGER unique NOT NULL  COMMENT 'Village ID',
    "VILLAGE_NAME" CHAR(20) NOT NULL COMMENT 'Store ID',
    PRIMARY KEY ("VILLAGE_ID")
) WITH ASSOCIATIONS(JOIN "RESIDENTS" AS "RES" ON "VILLAGE_ID" = "RES"."VILLAGE_ID" ) 

